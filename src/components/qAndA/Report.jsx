const { API_URL, API_KEY } = process.env;

import React from 'react';
import ReactDOM from 'react-dom';

import axios from 'axios';

export default function Report() {
  // const handleReport = axios.put(`${ API_URL }/qa/questions/`)

  return (
    <button>
      Report
    </button>
  );
}